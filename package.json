{
  "name": "namuga-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && npx --yes serve dist -l 8000"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}
